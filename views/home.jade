extends layout

block content
   if !(posts && posts.length)
      p Nothing to see here. Move along
   else
      ul.posts
         each post in posts
            li
                h2 #{post.title}
                p #{post.body}
                span #{post.created.toGMTString()}
                p
                    | by #{author}